
typedef enum _fuelsensor_outputType{LENGTH=0, VOLUME} FS_OUTPUTTYPE;

FS_OUTPUTTYPE f;
void setup() {
  // put your setup code here, to run once:
//f = (_fuelsensor_outputType)LENGTH;
//f = (FS_OUTPUTTYPE)LENGTH;
f = 2;
}

void loop() {
  // put your main code here, to run repeatedly:

}
